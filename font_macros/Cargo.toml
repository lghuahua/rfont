[package]
name = "font_macros"
version = "0.1.0"
edition = "2021"
description = "字体库的 proc-macro 宏定义"
authors = ["rfont Team"]
license = "MIT OR Apache-2.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
proc-macro = true

[dependencies]
proc-macro2 = { workspace = true }
quote = { workspace = true }
syn = { workspace = true }
